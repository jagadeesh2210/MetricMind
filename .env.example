# Week 4: local dev mode needs nothing — the app reads lib/data/raw_orders.csv
# and replicates the dbt fct_margin transform in-process. No keys required.

# Production path (not required for review): point at a real Cube.dev
# deployment sitting in front of the Snowflake fct_margin table.
# WAREHOUSE_MODE=cube
# CUBE_API_URL=https://your-cube-deployment.example.com/cubejs-api/v1
# CUBE_API_TOKEN=changeme
